# Safe Agent Fixture

Expected permit status: `approved`.

This fixture represents a small read-only agent. It has no MCP config, no credential references, no CI automation, and no external send path.

