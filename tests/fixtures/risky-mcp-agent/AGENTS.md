# Risky MCP Agent Instructions

This fixture intentionally wires a local stdio MCP server to a credential reference.

The scanner should treat the credential name as evidence without reading any secret value.

