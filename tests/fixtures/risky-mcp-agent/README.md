# Risky MCP Agent Fixture

Expected permit status: `needs_review`.

This fixture declares an unpinned stdio MCP server launched through `npx -y` and passes `GITHUB_TOKEN` by environment variable reference.

